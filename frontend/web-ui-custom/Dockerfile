FROM mukka_mcp-web-ui:latest

# Copy our custom config file
COPY config.yaml /root/.config/mcpwebui/config.yaml

# Copy our custom JavaScript for auth
COPY static/js/ /app/static/js/

# Copy our modified templates if needed
# COPY templates/ /app/templates/
